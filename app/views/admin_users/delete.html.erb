<%= link_to("<< Back to Index", {:action => 'index'}, :class => 'back-link') %>

<div class="sadmins delete">
  <h2>Delete Admin</h2>

  <%= form_for(:admin_user, :url => {:action => 'destroy', :id => @admin.id}) do |f| %>

    <p>Are you sure you want to permanently delete this admin?</p>

    <p class="reference-name"><%= @admin.username %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Admin") %>
    </div>

  <% end %>
</div>